import 'package:flutter/material.dart';

class RecycleTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recycle'),
      ),
      body: Center(
        child: Text('Recycle Content'),
      ),
    );
  }
} 